# Security Notes

Personal blog, built with Jekyll (minima theme) and published via GitHub Pages.

New posts go in `_posts/` named `YYYY-MM-DD-slug.md` with `layout: post` front matter. Push to `main` and GitHub Pages rebuilds automatically — no local toolchain needed.
